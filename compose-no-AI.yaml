services:
  backend:
    build:
      context: ../backend
    environment:
      NODE_ENV: production
    ports:
      - "3001:3001"

  frontend:
    build:
      context: ../frontend
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"

# Example: Add volumes or networks if needed
# volumes:
#   db-data: